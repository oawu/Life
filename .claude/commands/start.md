# 啟動專案

在後台啟動前端開發伺服器：

```bash
cd /Users/oa/Workspace/32_Life/frontend/cmd && npm run serve
```

- 前端：http://localhost:7100（port 7100）
- 後端：Docker 容器 `php` 已持續運行，無需額外啟動

請使用 `run_in_background: true` 在背景執行。
